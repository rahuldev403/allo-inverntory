# listner's-key

Next.js app for uploading books and chatting with them via AI, with Clerk auth.

## Local Development

```bash
npm install
npm run dev
```

Open http://localhost:3000

## Scripts

- `npm run dev` start the dev server
- `npm run build` build for production
- `npm run start` run the production build
